import { CustomValidator, Meta } from 'express-validator/lib/base-validator/lib/base';
import { Context } from 'express-validator/lib/contextlidator/lib/context';
import { ContextItem } from 'express-validator/lib/context-items/context-itemtor/lib/context-items/context-item';
export declare class CustomValidation implements ContextItem {
    private readonly validator;
    private readonly negated;
    message: any;
    constructor(validator: CustomValidator, negated: boolean);
    run(context: Context, value: any, meta: Meta): Promise<void>;
}
