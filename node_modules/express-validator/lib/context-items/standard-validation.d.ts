import { Meta, StandardValidator } from 'express-validator/lib/base-validator/lib/base';
import { toString as toStringImpl } from 'express-validator/lib/utilsvalidator/lib/utils';
import { Context } from 'express-validator/lib/contextlidator/lib/context';
import { ContextItem } from 'express-validator/lib/context-items/context-itemtor/lib/context-items/context-item';
export declare class StandardValidation implements ContextItem {
    private readonly validator;
    private readonly negated;
    private readonly options;
    private readonly stringify;
    message: any;
    constructor(validator: StandardValidator, negated: boolean, options?: any[], stringify?: typeof toStringImpl);
    run(context: Context, value: any, meta: Meta): Promise<void>;
}
