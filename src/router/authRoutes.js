/*const express = require('express');
const { signup ,login} = require('../Controller/authControllerfirebase');
const router = express.Router();


// Register
router.post("/signup", signup);

//router.post('/register',upload.single('file'),registerValidation,validate,register);//ok
// Login
router.post("/login",login );

//router.post('/login',loginValidation,validate,login);//ok

module.exports = router;*/
